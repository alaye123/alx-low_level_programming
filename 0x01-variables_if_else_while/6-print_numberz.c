#include <stdio.h>

/**
 * main - This Prints numbers from 0 to 9.
 *
 * Return: This is always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}

